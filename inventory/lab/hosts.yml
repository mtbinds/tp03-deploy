---
all:
  children:
    local:          # Groupe "local" : hôtes gérés en connexion directe (pas SSH)
      hosts:
        localhost:
          ansible_connection: local                      # Mode local : pas de SSH, exécution Python directe
          ansible_python_interpreter: "{{ ansible_playbook_python }}"
